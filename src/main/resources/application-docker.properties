# Docker PostgreSQL is initialized from the committed Supabase migrations.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.default_schema=app

# The backend port is private to Compose; only the trusted Nginx service proxies it.
server.forward-headers-strategy=framework
